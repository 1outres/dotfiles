set -g prefix C-a
unbind C-b

set-option -g mouse on

bind | split-window -h
bind - split-window -v

bind -n C-o select-pane -t :.+

bind -n C-left previous-window
bind -n C-right next-window

source "/home/cron/.local/lib/python3.10/site-packages/powerline/bindings/tmux/powerline.conf"
run-shell "powerline-daemon -q"
